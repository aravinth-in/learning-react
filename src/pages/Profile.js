import {ChangeProfile} from '../components/ChangeProfile';

export const Profile = () => {
    return (
        <div> 
            Profile Page, user:
            <ChangeProfile />
        </div>
    );
}